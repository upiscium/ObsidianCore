> [!danger]  OUT OF DATE
> ```tasks
> not done
> due before today
> short mode
> sort by due
> ```

> [!warning]  Today's tasks
> ```tasks
> not done
> due today
> short mode
> sort by due
> ```

> [!check] Upcoming(in 2weeks)
> ```tasks
> not done
> (scheduled before tomorrow)
> ((due after today) AND (due before in 15 days) AND (scheduled before tomorrow))
> sort by priority scheduled
> ```

> [!todo]- Under running
> ```tasks
> not done
> (scheduled before tomorrow) AND (no due date)
> sort by priority scheduled
> ```

> [!abstract]- Not listed
> ```tasks
> not done
> (no due date) AND (no scheduled date)
>```
